{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$id": "abb.eam.asset.utilities.transformer.secondaryattribute.txtype-1.0.0.json",
  "title": "Transformer Type Model",
  "description": "A specific model the TX type as a secondary attribute",

  "$comment": "This schema defines the object model for a transformer type",

  "type": "object",
  "properties": {
      "name": {
          "type": "string",
          "enum": [
            "txtype"
          ],
          "description": "The key used for transformer types is always 'txtype'"
      },
      "value": {
          "type": "object",
          "properties": {
            "txtype": {
              "enum": [
                "Power Transformer", "Distribution Transformer"
              ]
            },
            "manufacturer": {
              "enum": [
                "ABB", "GE", "Siemens"
              ]
            }
          },
          "description": "The TX type is a combination of manufacturer and physical type of TX"
      },
      "category": {
          "type": "string",
          "description": "This is the optional category that the attribute may fit in e.g. from a particular system or of a particular type"
      },
      "dataType": {
          "type": "string",
          "enum": [
            "abb.eam.asset.utilities.transformer.secondaryattribute.txtype-1.0.0.json"
          ],
          "description": "For TX types we always use this schema's name"
      }
  },
  "required": [
    "name", "value", "dataType"
  ]
}
