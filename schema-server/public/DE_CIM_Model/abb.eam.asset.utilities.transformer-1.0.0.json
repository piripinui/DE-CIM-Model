{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$id": "abb.eam.asset.utilities.transformer-1.0.0.json",
  "title": "Transformer Model",
  "description": "A specific model for Transformers that derives from the generic Asset model. It extends the generic model by adding a 'type' property",

  "$comment": "A specific model definition for a Power Transformer that inherits from the generic Asset definition",

  "allOf": [
    { "$ref": "abb.eam.asset-1.0.0.json" },
    { "properties": {
        "type": {
          "type": "object",
          "description": "The type of a Transformer is a combination of its manufacturer and type",
          "properties": {
            "txtype": {
              "type": "string",
              "enum": [
                "Power Transformer",
                "Distribution Transformer"
              ],
              "description": "The valid types of transformer"
            },
            "manufacturer": {
              "type": "string",
              "enum": [
                "ABB",
                "GE",
                "Siemens"
              ],
              "description": "The valid manufacturers of transformers"
            }
          }
        }
      }
    }
  ]
}
